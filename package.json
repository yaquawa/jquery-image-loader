{
  "name": "jquery-image-loader",
  "version": "0.0.3",
  "description": "Simple jQuery plugin for loading images.",
  "main": "src/ImageLoader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp && gulp uglify",
    "watch": "gulp watch"
  },
  "keywords": [
    "image-loader",
    "jquery"
  ],
  "author": "yaquawa",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "browserify-shim": "^3.8.12",
    "gulp": "^3.9.0",
    "gulp-rename": "^1.2.2",
    "gulp-replace": "^0.5.4",
    "gulp-uglify": "^1.5.1",
    "through2": "^2.0.0"
  },
  "dependencies": {
    "jquery": "^1.12.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "browserify-shim": {
    "jquery": "global:jQuery"
  }
}
